#include "Solver.h"
#include <iostream>
#include <map>

using namespace std;

int main() {
    map<int, int> resourceCounts;

    int numResources, shipCount, shipCapacity;
    int labCount, materialThreshold, quotaCount;

    cin >> numResources >> shipCount >> shipCapacity;

    cin >> labCount >> materialThreshold >> quotaCount;

    return 0;
}